import 'package:flutter/material.dart';

class NerScreen extends StatelessWidget {
  const NerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Scaffold(
     appBar: AppBar(
       title: const Text('New'),),
      body: const Text('New text'),);
  }
}
